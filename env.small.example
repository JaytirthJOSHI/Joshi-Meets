# Copy to .env on the VPS:  cp env.small.example .env  then edit.
#
MYSQL_ROOT_PASSWORD=use-a-long-random-password

# After GitHub pushes to Docker Hub (main branch):
JOSHI_MEETS_IMAGE=yourdockerhub/joshi-meets-server:dev
