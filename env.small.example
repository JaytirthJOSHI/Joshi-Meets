# Copy to .env on the VPS:  cp env.small.example .env  then edit MYSQL_ROOT_PASSWORD if needed.
#
MYSQL_ROOT_PASSWORD=use-a-long-random-password

# GitHub Container Registry — matches this repo after CI runs on main (owner + repo name lowercased):
JOSHI_MEETS_IMAGE=ghcr.io/jaytirthjoshi/joshi-meets:dev
